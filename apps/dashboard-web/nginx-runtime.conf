server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Static SPA — config.js is generated at container start by entrypoint.sh
  location /config.js { add_header Cache-Control "no-store"; }

  location / {
    try_files $uri /index.html;
  }
}
